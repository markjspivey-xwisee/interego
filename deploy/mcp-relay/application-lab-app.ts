/**
 * One generic, memory-free MCP App for every descriptor-defined Interego
 * application.  The open_application_lab tool supplies the live projection; this
 * file contains presentation and tool-call plumbing only.
 */

const STYLE = String.raw`
:root{--bg:#f5f3ed;--panel:#fffdf8;--ink:#171714;--muted:#6f6d64;--line:#dedbd0;--soft:#ece9df;--green:#08775a;--green-bg:#e6f5ef;--amber:#9a5b00;--amber-bg:#fff1d6;--red:#aa2929;--red-bg:#fde8e5;--blue:#3158a7;--shadow:0 14px 44px rgba(39,35,23,.08);font-synthesis:none}
@media(prefers-color-scheme:dark){:root{--bg:#121310;--panel:#191a16;--ink:#f0eee5;--muted:#aaa79b;--line:#35362f;--soft:#272821;--green:#69d9b2;--green-bg:#17392e;--amber:#ffc66d;--amber-bg:#483316;--red:#ff9b91;--red-bg:#45211e;--blue:#9eb8ff;--shadow:none}}
:root[data-theme=dark]{--bg:#121310;--panel:#191a16;--ink:#f0eee5;--muted:#aaa79b;--line:#35362f;--soft:#272821;--green:#69d9b2;--green-bg:#17392e;--amber:#ffc66d;--amber-bg:#483316;--red:#ff9b91;--red-bg:#45211e;--blue:#9eb8ff;--shadow:none}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:14px/1.5 ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif;-webkit-font-smoothing:antialiased}.shell{max-width:1100px;margin:0 auto;padding:18px}.hero{background:var(--panel);border:1px solid var(--line);border-radius:18px;padding:18px 20px;box-shadow:var(--shadow)}.eyebrow{font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:var(--muted)}h1{font:650 25px/1.15 ui-serif,Georgia,serif;letter-spacing:-.02em;margin:5px 0}.sub{color:var(--muted);max-width:760px}.hero-row,.toolbar,.badges,.tabs,.actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.hero-row{justify-content:space-between;align-items:flex-start}.toolbar{margin-top:14px}.toolbar select,.toolbar button,.action input,.action select,.action textarea{font:inherit;color:var(--ink);background:var(--panel);border:1px solid var(--line);border-radius:9px;padding:8px 10px}.toolbar select{max-width:560px}.button{appearance:none;border:1px solid var(--line);border-radius:9px;background:var(--panel);color:var(--ink);font:inherit;font-weight:700;padding:8px 12px;cursor:pointer}.button.primary{background:var(--ink);color:var(--panel);border-color:var(--ink)}.button:disabled{opacity:.5;cursor:not-allowed}.badge{display:inline-flex;align-items:center;gap:5px;border-radius:999px;padding:4px 8px;font-size:11px;font-weight:750;background:var(--soft);color:var(--muted)}.badge.ok{background:var(--green-bg);color:var(--green)}.badge.warn{background:var(--amber-bg);color:var(--amber)}.badge.bad{background:var(--red-bg);color:var(--red)}.dot{width:6px;height:6px;border-radius:50%;background:currentColor}.grid{display:grid;grid-template-columns:minmax(0,1fr) 280px;gap:14px;margin-top:14px}.main,.side,.card{background:var(--panel);border:1px solid var(--line);border-radius:15px}.main{min-width:0}.side{padding:15px;height:max-content}.tabs{padding:8px 10px 0;border-bottom:1px solid var(--line)}.tab{appearance:none;border:0;border-bottom:2px solid transparent;background:none;color:var(--muted);padding:8px 10px;font:inherit;font-size:12px;font-weight:750;cursor:pointer;margin-bottom:-1px}.tab.on{color:var(--ink);border-color:var(--ink)}.pane{display:none;padding:16px}.pane.on{display:block}.section-head{display:flex;align-items:baseline;justify-content:space-between;gap:10px;margin:0 0 12px}.section-head h2,.side h2{font-size:14px;margin:0;letter-spacing:-.01em}.count{color:var(--muted);font-size:11px}.view{border-top:1px solid var(--line);padding:12px 0}.view:first-child{border-top:0;padding-top:0}.view h3{font-size:12px;color:var(--muted);margin:0 0 7px}.value{font:650 23px/1.2 ui-serif,Georgia,serif}.tree,pre{margin:0;background:var(--soft);border-radius:10px;padding:11px;white-space:pre-wrap;word-break:break-word;font:12px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace}.table-wrap{max-width:100%;overflow:auto}table{width:100%;border-collapse:collapse;font-size:12px}th,td{text-align:left;vertical-align:top;border-bottom:1px solid var(--line);padding:7px 8px}th{color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.06em}.action{border:1px solid var(--line);border-radius:12px;padding:13px;margin:10px 0}.action h3{font-size:14px;margin:0}.action p{color:var(--muted);font-size:12px;margin:5px 0 10px}.action-top{display:flex;align-items:flex-start;justify-content:space-between;gap:8px}.field{margin:9px 0}.field label{display:block;font-size:11px;font-weight:750;margin-bottom:3px}.action input,.action select,.action textarea{width:100%;background:var(--bg)}.action textarea{min-height:64px;resize:vertical}.guard{font-size:11px;color:var(--muted);margin:7px 0}.guard.ok{color:var(--green)}.guard.bad{color:var(--red)}.confirm{background:var(--amber-bg);border:1px dashed var(--amber);padding:10px;border-radius:10px;margin-top:9px}.timeline{position:relative;margin-left:7px}.link{border-left:2px solid var(--line);padding:0 0 14px 18px;position:relative}.link:before{content:"";position:absolute;left:-6px;top:2px;width:9px;height:9px;border-radius:50%;background:var(--green);box-shadow:0 0 0 3px var(--panel)}.link.bad:before{background:var(--red)}.link:last-child{padding-bottom:0}.link-title{font-weight:750}.mono{font:11px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace;word-break:break-all;color:var(--muted)}.side dl{margin:8px 0 16px}.side dt{color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.06em;margin-top:9px}.side dd{margin:2px 0;word-break:break-word;font-size:12px}.check{display:grid;grid-template-columns:16px 1fr;gap:7px;margin:8px 0;font-size:12px}.check .mark{font-weight:900;color:var(--green)}.check.bad .mark{color:var(--red)}.status{font-size:12px;margin-top:8px;color:var(--muted)}.status.ok{color:var(--green)}.status.bad{color:var(--red)}.empty{color:var(--muted);padding:28px;text-align:center}.fatal{margin:18px;background:var(--red-bg);color:var(--red);border-radius:12px;padding:14px;white-space:pre-wrap}.hidden{display:none!important}@media(max-width:760px){.grid{grid-template-columns:1fr}.shell{padding:9px}.hero{border-radius:14px}.side{order:-1}.toolbar select{width:100%;max-width:none}}
`;

const BOOT_JS = String.raw`
var DATA=null,RPC=1,PENDING={},PREVIEW_EPOCH=0,REFRESH_REQUEST=0;
function q(id){return document.getElementById(id)}
function el(tag,cls,text){var e=document.createElement(tag);if(cls)e.className=cls;if(text!==undefined&&text!==null)e.textContent=String(text);return e}
function isLab(d){return !!d&&typeof d==='object'&&d.kind==='interego.application-lab/v1'}
function unwrap(r){if(r&&isLab(r.structuredContent))return r.structuredContent;if(isLab(r))return r;if(r&&r.result&&isLab(r.result.structuredContent))return r.result.structuredContent;return null}
function initialOutput(){try{return window.openai&&window.openai.toolOutput}catch(e){return null}}
function callTool(name,args){try{if(window.openai&&typeof window.openai.callTool==='function')return Promise.resolve(window.openai.callTool(name,args))}catch(e){}return new Promise(function(resolve,reject){var id='lab-'+RPC++;PENDING[id]={resolve:resolve,reject:reject};window.parent.postMessage({jsonrpc:'2.0',id:id,method:'tools/call',params:{name:name,arguments:args}},'*');setTimeout(function(){if(PENDING[id]){delete PENDING[id];reject(new Error('timed out waiting for '+name))}},60000)})}
window.addEventListener('message',function(ev){if(ev.source!==window.parent)return;var m=ev.data;if(!m||typeof m!=='object')return;if(m.method==='ui/notifications/tool-result'){var d=m.params&&m.params.structuredContent;if(isLab(d))hydrate(d)}else if(m.id!==undefined&&PENDING[m.id]){var p=PENDING[m.id];delete PENDING[m.id];if(m.error)p.reject(new Error(m.error.message||'tool error'));else p.resolve(m.result)}})
window.addEventListener('openai:set_globals',function(ev){var g=ev&&ev.detail&&ev.detail.globals||{};if(g.toolOutput===undefined)return;var d=unwrap(g.toolOutput)||g.toolOutput;if(isLab(d))hydrate(d)},{passive:true})
function local(iri){var s=String(iri||'');return s.split(/[#:]/).pop()||s}
function short(s,n){s=String(s||'');n=n||16;return s.length>n*2+3?s.slice(0,n)+'...'+s.slice(-n):s}
function path(obj,p){if(!p)return obj;var cur=obj;String(p).split('.').forEach(function(k){cur=cur==null?undefined:cur[k]});return cur}
function stringify(v){return JSON.stringify(v,null,2)}
function badge(text,kind){var b=el('span','badge '+(kind||''));b.appendChild(el('span','dot'));b.appendChild(document.createTextNode(text));return b}
function setStatus(text,kind){var s=q('global-status');s.className='status '+(kind||'');s.textContent=text||''}
function hydrate(d){if(!isLab(d))return;PREVIEW_EPOCH++;DATA=d;render()}
function switchTab(name){['state','actions','replay','provenance'].forEach(function(t){q('tab-'+t).classList.toggle('on',t===name);q('pane-'+t).classList.toggle('on',t===name)})}
function render(){var d=DATA;if(!d)return;q('fatal').classList.add('hidden');q('content').classList.remove('hidden');var app=d.application||{},head=d.head||{},trust=d.trust||{},replay=d.replay||{},def=app.definition||{},ui=def.ui||{},views=Array.isArray(ui.views)?ui.views:[],primary=views.find(function(v){return v.id===ui.primaryView}),primaryValue=primary?path(head.state,primary.path):undefined;q('title').textContent=app.title||'Application Lab';q('subtitle').textContent=app.description||'A live application projected from signed Interego graphs.';q('version').textContent=app.version?'v'+app.version:'';var bs=q('badges');bs.innerHTML='';bs.appendChild(badge(d.live?'Live':'Snapshot',d.live?'ok':'warn'));bs.appendChild(badge(trust.verified?'Fully verified':'Verification incomplete',trust.verified?'ok':'bad'));bs.appendChild(badge((replay.verifiedLinks||0)+'/'+(replay.chainLength||0)+' replay links',replay.complete?'ok':'bad'));var sel=q('app-select');sel.innerHTML='';((d.catalog&&d.catalog.applications)||[]).forEach(function(a){var o=el('option',null,a.title||a.applicationId);o.value=a.applicationId;if(a.applicationId===app.applicationId)o.selected=true;sel.appendChild(o)});q('head-status').textContent=primaryValue===undefined?'v'+head.version:String(primaryValue);q('head-version').textContent='state v'+head.version;q('head-cid').textContent=head.cid;q('head-url').textContent=head.descriptorUrl;q('actor').textContent=((d.actions||[]).some(function(a){return a.executable})?'Bound session':'Read-only / no eligible action');renderState(d);renderActions(d);renderReplay(d);renderProvenance(d);renderSide(d);setStatus('Resolved live at '+new Date(d.generatedAt).toLocaleString(),'ok')}
function renderState(d){var box=q('state-views');box.innerHTML='';var def=d.application&&d.application.definition||{},ui=def.ui||{},views=Array.isArray(ui.views)?ui.views:[],state=d.head&&d.head.state||{};if(!views.length){var pre=el('pre');pre.textContent=stringify(state);box.appendChild(pre);return}views.forEach(function(v){var wrap=el('section','view');wrap.appendChild(el('h3',null,v.label||v.id||v.path));var value=path(state,v.path);if(v.kind==='value'){wrap.appendChild(el('div','value',value===undefined?'—':value))}else if(v.kind==='table'&&Array.isArray(value)){var tw=el('div','table-wrap'),table=el('table'),thead=el('thead'),tr=el('tr');var cols=Array.isArray(v.columns)?v.columns:[];cols.forEach(function(c){tr.appendChild(el('th',null,c.label||c.id))});thead.appendChild(tr);table.appendChild(thead);var tb=el('tbody');value.forEach(function(row){var rr=el('tr');cols.forEach(function(c){var x=path(row,c.path);rr.appendChild(el('td',null,x===undefined?'':typeof x==='object'?stringify(x):x))});tb.appendChild(rr)});table.appendChild(tb);tw.appendChild(table);wrap.appendChild(tw)}else{var pre=el('pre','tree');pre.textContent=stringify(value);wrap.appendChild(pre)}box.appendChild(wrap)})}
function inputFor(spec){var inp;if(Array.isArray(spec.options)){inp=el('select');spec.options.forEach(function(v){var o=el('option',null,v);o.value=String(v);inp.appendChild(o)})}else if(spec.type==='boolean'){inp=el('select');['true','false'].forEach(function(v){var o=el('option',null,v);o.value=v;inp.appendChild(o)})}else{inp=el(spec.type==='text'?'textarea':'input');if(inp.tagName==='INPUT')inp.type=spec.type==='number'?'number':'text'}inp.setAttribute('data-name',spec.name);inp.setAttribute('data-type',spec.type||'string');return inp}
function collect(card,inputs){var p={};inputs.forEach(function(s){var i=card.querySelector('[data-name="'+CSS.escape(s.name)+'"]');if(!i)return;var v=i.value;if(v===''&&!s.required)return;if(s.type==='number')v=Number(v);else if(s.type==='boolean')v=v==='true';p[s.name]=v});return p}
function renderActions(d){var box=q('action-list');box.innerHTML='';var list=d.actions||[];q('action-count').textContent=list.length+' declared';if(!list.length){box.appendChild(el('div','empty','No actions are declared by the active signed contract.'));return}list.forEach(function(a){var card=el('article','action'),top=el('div','action-top'),left=el('div');left.appendChild(el('h3',null,a.label||local(a.actionIri)));left.appendChild(el('div','mono',a.method+' · '+a.target));top.appendChild(left);top.appendChild(badge(a.executable?'Available':'Blocked',a.executable?'ok':'warn'));card.appendChild(top);if(a.description)card.appendChild(el('p',null,a.description));var evidence=Array.isArray(a.evidence)?a.evidence:[];if(evidence.length){var ev=el('pre','tree');ev.textContent='Verified evidence contract\n'+evidence.map(function(x){return '• '+(x.role||x.input)+' · '+(x.documentType||'signed JSON')+(x.requireCurrentHead===false?' · historical accepted':' · current unforked head')}).join('\n');card.appendChild(ev)}var guardText=a.guardDeferred?'Guard is evaluated after verified form input and evidence':((a.guardPass?'Guard passes · ':'Guard blocks · ')+(a.guardExplanation||a.disabledReason));var guard=el('div','guard '+(a.guardDeferred||a.guardPass?'ok':'bad'),guardText);card.appendChild(guard);var inputs=Array.isArray(a.inputs)?a.inputs:[];inputs.forEach(function(s){var f=el('div','field'),lab=el('label',null,(s.label||s.name)+(s.required?' *':'')),inp=inputFor(s);inp.disabled=!(d.trust&&d.trust.verified);f.appendChild(lab);f.appendChild(inp);card.appendChild(f)});var row=el('div','actions'),go=el('button','button '+(String(a.method).toUpperCase()==='POST'?'primary':''),String(a.method).toUpperCase()==='GET'?'Refresh verified state':(a.label||local(a.actionIri)));go.disabled=!a.executable;row.appendChild(go);card.appendChild(row);var conf=el('div','confirm hidden'),status=el('div','status');card.appendChild(conf);card.appendChild(status);addPreview(card,row,d,a,inputs);go.addEventListener('click',function(){if(String(a.method).toUpperCase()==='GET'){refresh();return}var payload=collect(card,inputs);conf.innerHTML='';conf.classList.remove('hidden');conf.appendChild(el('strong',null,'Confirm exact descriptor-bound action'));var pre=el('pre');pre.textContent=stringify({actionIri:a.actionIri,expectedHead:d.head.cid,payload:payload,evidenceContract:evidence});conf.appendChild(pre);var rr=el('div','actions'),yes=el('button','button primary','Confirm & sign'),no=el('button','button','Cancel');rr.appendChild(yes);rr.appendChild(no);conf.appendChild(rr);no.addEventListener('click',function(){conf.classList.add('hidden')});yes.addEventListener('click',function(){yes.disabled=true;no.disabled=true;status.className='status';status.textContent='Re-resolving head, contract and external evidence...';callTool('execute_application_action',{catalog_graph_iri:d.catalog.graphIri,catalog_descriptor_url:d.catalog.descriptorUrl,application_id:d.application.applicationId,action_iri:a.actionIri,expected_head:d.head.cid,payload:payload}).then(function(r){var sc=r&&r.structuredContent||r&&r.result&&r.result.structuredContent||r;if(sc&&sc.error)throw new Error(sc.message||sc.error);status.className='status ok';status.textContent='Committed. Refreshing verified replay...';return refresh()}).catch(function(e){status.className='status bad';status.textContent='Refused: '+(e&&e.message||e);yes.disabled=false;no.disabled=false})})});box.appendChild(card)})}
function addPreview(card,row,d,a,inputs){
  if(String(a.method).toUpperCase()!=='POST')return;
  var button=el('button','button','Preview changes'),output=el('div','preview'),serial=0;
  button.disabled=!(d.trust&&d.trust.verified&&d.application.contractDigest);
  row.appendChild(button);card.appendChild(output);
  card.addEventListener('input',function(){serial++;output.innerHTML='';button.disabled=!(d.trust&&d.trust.verified&&d.application.contractDigest)});
  button.addEventListener('click',function(){
    var request=++serial,epoch=PREVIEW_EPOCH;
    button.disabled=true;output.innerHTML='';output.appendChild(el('div','status','Verifying live authority and previewing...'));
    var args={catalog_graph_iri:d.catalog.graphIri,catalog_descriptor_url:d.catalog.descriptorUrl,application_id:d.application.applicationId,action_iri:a.actionIri,expected_head:d.head.cid,expected_contract_digest:d.application.contractDigest,payload:collect(card,inputs)};
    function current(){return request===serial&&epoch===PREVIEW_EPOCH&&DATA===d&&q('app-select').value===d.application.applicationId}
    callTool('preview_application_action',args).then(function(r){
      if(!current())return;
      var sc=r&&r.structuredContent||r&&r.result&&r.result.structuredContent||r;
      if(sc&&sc.error)throw new Error(sc.message||sc.error);
      if(!sc||sc.kind!=='application-simulation'||sc.live!==true||sc.committed!==false||!sc.basis||sc.basis.stateHead!==d.head.cid||sc.basis.contractDigest!==d.application.contractDigest||sc.basis.applicationId!==d.application.applicationId)throw new Error('Preview does not match the displayed authority');
      var result=sc.alternatives&&sc.alternatives[0];
      if(!result||result.actionIri!==a.actionIri)throw new Error('Preview returned no matching action');
      output.innerHTML='';output.appendChild(el('div','status '+(result.status==='simulated'?'ok':'bad'),result.status==='simulated'?'Preview only — no changes committed':'Refused: '+(result.reason||'Action needs input')));
      output.appendChild(el('div','mono','State v'+d.head.version+' · '+short(sc.basis.stateHead,12)+' · '+sc.basis.actor+' · '+sc.basis.at));
      if(result.status==='simulated'){
        output.appendChild(el('pre','tree',stringify(result.changes)));
        var details=el('details'),summary=el('summary',null,'Proposed successor');details.appendChild(summary);details.appendChild(el('pre','tree',stringify(result.successor)));output.appendChild(details);
      }
    }).catch(function(e){if(current()){output.innerHTML='';output.appendChild(el('div','status bad','Preview refused: '+(e&&e.message||e)))}}).finally(function(){if(current())button.disabled=false});
  });
}
function renderReplay(d){var r=d.replay||{},box=q('timeline');box.innerHTML='';q('replay-summary').textContent=(r.complete?'Complete deterministic replay':'Replay incomplete')+' · '+(r.chainLength||0)+' links · '+((r.contractEpochs||[]).length)+' contract epoch(s)';(r.links||[]).forEach(function(l){var n=el('div','link '+(l.verified?'':'bad'));n.appendChild(el('div','link-title','v'+l.version+' · '+(l.actionIri?local(l.actionIri):'genesis')+' · '+(l.verified?'verified':'failed')));if(l.actor)n.appendChild(el('div','mono',short(l.actor,22)));if(l.contractDigest)n.appendChild(el('div','mono','contract '+short(l.contractDigest,12)+(l.contractVersion?' · v'+l.contractVersion:'')));if(l.evidenceCount)n.appendChild(el('div','mono',l.evidenceCount+' external evidence descriptor(s) · '+(l.evidenceVerified?'verified':'failed')));n.appendChild(el('div','mono',short(l.cid,16)));if(l.errors&&l.errors.length)n.appendChild(el('div','status bad',l.errors.join(' · ')));box.appendChild(n)})}
function renderProvenance(d){var box=q('prov-table');box.innerHTML='';var table=el('table'),head=el('tr');['Artifact','Signer','Bindings','Digest','Result'].forEach(function(x){head.appendChild(el('th',null,x))});var th=el('thead');th.appendChild(head);table.appendChild(th);var body=el('tbody');(d.provenance||[]).forEach(function(p){var tr=el('tr');tr.appendChild(el('td',null,p.role));tr.appendChild(el('td','mono',short(p.signedBy,17)));tr.appendChild(el('td',null,(p.authorshipVerified?'signature ':'')+(p.contentBinding==='bound'?'content ':'')+(p.descriptorBinding?'descriptor':'')));tr.appendChild(el('td',null,p.digestVerified?'match':'mismatch'));tr.appendChild(el('td',null,p.trusted?'verified':'failed'));body.appendChild(tr)});table.appendChild(body);box.appendChild(table)}
function renderSide(d){var m=d.manifestChecks||{},checks=q('checks');checks.innerHTML='';Object.keys(m).forEach(function(k){var c=m[k]||{},ok=c.descriptorMatch&&c.digestMatch&&c.cidMatch!==false,wrap=el('div','check '+(ok?'':'bad'));wrap.appendChild(el('span','mark',ok?'✓':'×'));wrap.appendChild(el('span',null,k+': URL '+(c.descriptorMatch?'matches':'differs')+', digest '+(c.digestMatch?'matches':'differs')+', CID '+(c.cidMatch===false?'differs':'matches')));checks.appendChild(wrap)});q('catalog-url').textContent=d.catalog.descriptorUrl;q('state-graph').textContent=d.application.stateGraphIri;q('contract-graph').textContent=d.application.contractGraphIri;q('governance-graph').textContent=d.application.governanceGraphIri||'none';var epochs=q('epochs');epochs.innerHTML='';((d.replay&&d.replay.contractEpochs)||[]).forEach(function(e){var wrap=el('div','check');wrap.appendChild(el('span','mark','✓'));wrap.appendChild(el('span',null,'Contract '+(e.version||short(e.digest,8))+' · '+e.links+' transition(s)'));epochs.appendChild(wrap)});if(!epochs.children.length)epochs.appendChild(el('div','count','Genesis only; no transition epoch yet.'))}
function argsForCurrent(){return DATA?{catalog_graph_iri:DATA.catalog.graphIri,catalog_descriptor_url:DATA.catalog.descriptorUrl,application_id:q('app-select').value,pod_url:DATA.catalog.podUrl}: {}}
function refresh(){PREVIEW_EPOCH++;var refreshRequest=++REFRESH_REQUEST;setStatus('Discovering and re-verifying live graphs...','');q('refresh').disabled=true;return callTool('open_application_lab',argsForCurrent()).then(function(r){var raw=r&&r.structuredContent||r&&r.result&&r.result.structuredContent||r;if(raw&&raw.error)throw new Error(raw.message||raw.error);var d=unwrap(r);if(!d)throw new Error('tool returned no Application Lab document');if(refreshRequest===REFRESH_REQUEST)hydrate(d);return d}).catch(function(e){if(refreshRequest===REFRESH_REQUEST)setStatus('Refresh failed: '+(e&&e.message||e),'bad');throw e}).then(function(v){if(refreshRequest===REFRESH_REQUEST)q('refresh').disabled=false;return v},function(e){if(refreshRequest===REFRESH_REQUEST)q('refresh').disabled=false;throw e})}
function fatal(msg){q('content').classList.add('hidden');var f=q('fatal');f.classList.remove('hidden');f.textContent=msg}
(function(){['state','actions','replay','provenance'].forEach(function(t){q('tab-'+t).addEventListener('click',function(){switchTab(t)})});q('refresh').addEventListener('click',function(){refresh().catch(function(){})});q('app-select').addEventListener('change',function(){refresh().catch(function(){})});var first=unwrap(initialOutput())||initialOutput();if(isLab(first))hydrate(first);else fatal('Waiting for the live Application Lab projection...')})();
`;

export const APPLICATION_LAB_APP_HTML = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Interego Application Lab</title><style>${STYLE}</style></head><body><div class="shell"><section class="hero"><div class="hero-row"><div><div class="eyebrow">Interego · generic authoritative application</div><h1 id="title">Application Lab</h1><div class="sub" id="subtitle">Resolving signed graphs…</div></div><div class="badges" id="badges"></div></div><div class="toolbar"><select id="app-select" aria-label="Application"></select><span id="version" class="badge"></span><button class="button" id="refresh">Rediscover & verify</button></div><div class="status" id="global-status"></div></section><div id="fatal" class="fatal hidden"></div><div id="content" class="grid hidden"><main class="main"><nav class="tabs" aria-label="Application Lab views"><button class="tab on" id="tab-state">State</button><button class="tab" id="tab-actions">Actions</button><button class="tab" id="tab-replay">Replay</button><button class="tab" id="tab-provenance">Provenance</button></nav><section class="pane on" id="pane-state"><div class="section-head"><h2>Authoritative state</h2><span class="count" id="head-version"></span></div><div id="state-views"></div></section><section class="pane" id="pane-actions"><div class="section-head"><h2>Descriptor-bound actions</h2><span class="count" id="action-count"></span></div><div id="action-list"></div></section><section class="pane" id="pane-replay"><div class="section-head"><h2>Complete replay</h2><span class="count" id="replay-summary"></span></div><div class="timeline" id="timeline"></div></section><section class="pane" id="pane-provenance"><div class="section-head"><h2>Cryptographic provenance</h2></div><div class="table-wrap" id="prov-table"></div></section></main><aside class="side"><h2>Resolved authority</h2><dl><dt>Current value</dt><dd id="head-status"></dd><dt>Head CID</dt><dd class="mono" id="head-cid"></dd><dt>Head descriptor</dt><dd class="mono" id="head-url"></dd><dt>Session</dt><dd id="actor"></dd></dl><h2>Catalog pins</h2><div id="checks"></div><h2>Contract epochs</h2><div id="epochs"></div><h2>Graph addresses</h2><dl><dt>Catalog</dt><dd class="mono" id="catalog-url"></dd><dt>State</dt><dd class="mono" id="state-graph"></dd><dt>Active contract</dt><dd class="mono" id="contract-graph"></dd><dt>Governance</dt><dd class="mono" id="governance-graph"></dd></dl></aside></div></div><script>${BOOT_JS}</script></body></html>`;
