{
  "name": "@interego/css-gate",
  "version": "0.2.0",
  "description": "CSS write-gate — reverse-proxy + per-user bearer write gating in front of the Community Solid Server. Speaks ALPN h2 to upstream via undici with a pooled keep-alive Client; preserves caller-supplied Host header so CSS's identifier-space check passes when the gate is talking to CSS over an internal hostname.",
  "type": "module",
  "private": true,
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "undici": "^7.0.0"
  }
}
