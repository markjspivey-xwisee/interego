# Known-drift allowlist for ontology-lint.
#
# Entries are `prefix:Term` that the lint would otherwise fail on.
# Migrate OUT by either (a) defining the term in docs/ns/<prefix>.ttl
# or (b) renaming the code to use an existing term or a non-owned
# namespace. A growing list is a signal to schedule an ontology pass.

# ── code: ──
# False positive from regex tokenization: the string
# `urn:agent:claude-code:local` embeds the literal "code:" inside a
# URN, not an RDF CURIE reference. The URN itself is a default env
# var identifier, not a code: ontology term.
code:local

# ── soc2: ──
# False positives from regex tokenization. SOC 2 control IRIs use
# dotted identifiers (CC6.1, P1.1, etc.) but the lint's term regex
# stops at the dot, reporting the prefix as "soc2:CC6". The actual
# fully-qualified terms (soc2:CC6.1, etc.) ARE defined in
# docs/ns/soc2.ttl.
soc2:CC1
soc2:CC2
soc2:CC3
soc2:CC4
soc2:CC5
soc2:CC6
soc2:CC7
soc2:CC8
soc2:CC9
soc2:C1
soc2:P1
# P4.2 referenced in docs/policies for data subject deletion control mapping.
soc2:P4

# ── iep: ──
# Reference in a comment in deploy/mcp-relay/server.ts to a
# placeholder predicate (iep:evidenceForControl) that's mentioned as a
# future extension to ManifestEntry. Not actually emitted to RDF.
iep:evidenceForControl
# CGContextType.Causal / .Projection in packages/core/src/rdf/namespaces.ts
# are emitted via `${CG}Causal` / `${CG}Projection` template literals.
# The corresponding iep:CausalFacet / iep:ProjectionFacet classes ARE
# defined in docs/ns/cg.ttl; the bare context-type individuals are not
# yet declared. Pre-existing drift surfaced by the template-literal
# scan extension.
iep:Causal
iep:Projection

# ── align: ──
# False positive from CSS text-align property in HTML style attributes
# (e.g., style="text-align:center"). The lint's term regex matches
# `align:center` because the negative lookbehind doesn't reject the
# hyphen in `text-align`. This is CSS, not an RDF ontology reference.
align:center
# PGSL projection-layer terms emitted by packages/pgsl/src/projection.ts +
# packages/solid/src/client.ts (pre-existing drift, carried through the
# Interego-Protocol rename; declare in iep.ttl when the projection vocab lands).
iep:EncryptedHolon
iep:XFacet
iep:contentCid
iep:encryptedHolon
iep:ofDescriptor
iep:pgslLevel
iep:pgslUri

# ── applications/ baseline (surfaced when the vertical layer was added to
#    SCAN_PATHS so NEW vertical drift now fails). These are PRE-EXISTING
#    terms emitted by verticals' use of core/L2/L3 prefixes; several are
#    genuine concepts that should be DECLARED in their docs/ns/<prefix>.ttl
#    (e.g. pgsl:hasAtom / pgsl:Atoms / pgsl:sourceFormat → pgsl; the
#    eu-ai-act / nist-rmf article+function individuals; passport:DelegationCredential;
#    amta:* rating predicates; abac:Policy) and a few are plurals/typos to
#    reconcile (iep:Affordances→Affordance). Tracked for a dedicated ontology
#    pass; not introduced by this remediation.
abac:Policy
amta:attestsTo
amta:axis
amta:fromExecution
amta:rating
eu-ai-act:Article12
eu-ai-act:Article15
iep:Affordances
iep:CapabilityCatalog
iep:Finding
iep:NamedGraph
iep:StandardsExtension
iep:Verification
iep:discover
iep:recordedAt
nist-rmf:MG
passport:DelegationCredential
pgsl:Atoms
pgsl:hasAtom
pgsl:sourceFormat
# deprecated cg: read-alias used by a feasibility tool (migrate to iep:ContextDescriptor)
cg:ContextDescriptor
