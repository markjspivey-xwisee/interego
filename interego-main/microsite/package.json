{
  "name": "@interego/main-site",
  "version": "0.1.0",
  "private": true,
  "description": "Top-level Interego site — what the substrate is, demos that run against the live deployment, and entry points into every vertical (Foxxi, LPC, ADP, AC, OWM, lrs-adapter).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
