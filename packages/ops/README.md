# @interego/ops

SOC 2 operational evidence event builders (deploys / access changes / wallet rotations / incidents / quarterly reviews). Emits @interego/core descriptors via @interego/compliance.

Particular composition over the `@interego/core` substrate — see
`docs/ARCHITECTURAL-FOUNDATIONS.md §12` for the substrate-vs-vertical
split that motivates this package boundary.
