{
  "name": "@interego/agent-interop",
  "version": "0.2.0",
  "description": "Spec-blind agent-interop engagement engine. A general work-record + card-projection engine whose state machine, card field mapping, wire routes and error table are all read from a declarative profile. A2A is the first profile — data, not code.",
  "author": "Mark Spivey",
  "license": "MIT",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" }
  },
  "files": ["dist/", "src/", "README.md"],
  "scripts": { "build": "tsc", "watch": "tsc --watch", "clean": "rm -rf dist/" },
  "engines": { "node": ">=20.0.0" },
  "dependencies": { "@interego/core": "*" },
  "devDependencies": { "@types/node": "^22.0.0", "typescript": "^5.7.0" }
}
